deployment:
  production:
    branch: rails
    commands:
      - git push git@heroku.com:snap-it-up-rails.git $CIRCLE_SHA1:refs/heads/master :
          timeout: 400
      - heroku run rake db:migrate --app snap-it-up-rails :
          timeout: 400
